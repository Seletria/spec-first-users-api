services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: spec_first_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
